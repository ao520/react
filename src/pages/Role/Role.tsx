

import React, { FC } from 'react'

const Role:FC<any> = ({children}) => {
  return (
    <div>
        <div>
            {children}
        </div>
    </div>
  )
}

export default Role